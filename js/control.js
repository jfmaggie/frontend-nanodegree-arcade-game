
//gamelevel is determined by speed.
//generate a random number
function randomNumber(a){
    var num = Math.random();
    num = Math.floor(a * num);
    return num;
}

// achievement info display
// $('.col-md-4').append("<div class='text-center achievement'>Achievement</div>");
